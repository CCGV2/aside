# Aside 领域语言

- **节目（Episode）**：原音频及其可复用的分析结果。与某一次收听的进度、问答分开。
- **收听会话（ListeningSession）**：协调原音频、插话方式、设备连接和自然续播。完整用户操作由会话执行，页面不协调底层调用顺序。
- **一轮问答（Conversation turn）**：从用户开始提问到回答完成或被取消。持有请求有效性、字幕合并、工具委派、回答与进度语音的分类。
- **插话（Interruption）**：一次离开节目展开对话的过程，可包含多轮问答；连续追问保留同一个续播锚点。
- **续播锚点（ResumeAnchor）**：打断前的自然语义落点。使用节目原音频的毫秒坐标。
- **追问等待（Follow-up window）**：回答完成后留给用户的时间；等待、取消和暂缓由应用决定，模型不解释沉默。
- **按需语音（On-demand voice）**：本地麦克风和云端连接拥有独立生命周期。首次自动插话与手动录音走本地转录交接，已连接的自动追问走实时音频。
- **问答任务（Question task）**：后端在已听内容范围内解释问题、执行工具并收集来源。供应商 Adapter 转换模型协议，不决定工具执行和续播政策。

时间、浏览器音频、模型传输均可用测试 Adapter 替换。测试替身通过不等于真实模型听感通过。
